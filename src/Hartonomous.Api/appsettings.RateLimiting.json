{
  "RateLimiting": {
    "TenantTiers": {
      "_comment": "Configure rate limits per tenant tier (requests per minute)",
      "Free": 10,
      "Basic": 100,
      "Premium": 500,
      "Enterprise": 2000,
      "Admin": 2147483647
    }
  },
  "_notes": [
    "This configuration is optional. TenantRateLimitPolicy has built-in defaults.",
    "Tier is extracted from 'tenant_tier' or 'extension_TenantTier' claim in Azure AD token.",
    "Admin role users bypass rate limits regardless of tier.",
    "Rate limit policies available:",
    "  - 'tenant-tier': Fixed window based on tenant tier",
    "  - 'tenant-api': Sliding window for API endpoints",
    "  - 'tenant-inference': Token bucket for inference/generation endpoints",
    "Usage: [EnableRateLimiting(\"tenant-tier\")]"
  ]
}
